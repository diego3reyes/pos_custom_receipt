<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Plantilla HTML base del ticket (solo se escribe en instalación inicial) -->
    <data noupdate="1">
        <record id="pos_custom_receipt_default_template" model="ir.config_parameter">
            <field name="key">pos_custom_receipt.default_template</field>
            <field name="value"><![CDATA[<div class="pos-receipt">

  <!-- ENCABEZADO -->
  <div style="text-align:center;">
    {% if company.logo %}
      <img src="{{ company.logo }}" style="max-width:150px; margin-bottom:8px;" />
    {% endif %}
    <div><strong>{{ company.name }}</strong></div>
    {% if company.vat %}
      <div>NIT: {{ company.vat }}</div>
    {% endif %}
    {% if company.street %}
      <div>{{ company.street }}</div>
    {% endif %}
    {% if company.phone %}
      <div>Tel: {{ company.phone }}</div>
    {% endif %}
  </div>

  <hr/>

  <!-- DATOS DEL TICKET -->
  <div>
    <div>Ticket: <strong>{{ ticket.number }}</strong></div>
    <div>Fecha: {{ ticket.date }} {{ ticket.time }}</div>
    {% if config.show_cashier %}
      <div>Cajero: {{ ticket.cashier }}</div>
    {% endif %}
    {% if config.show_customer %}
      <div>Cliente: {{ customer.name }}</div>
    {% endif %}
  </div>

  <hr/>

  <!-- PRODUCTOS -->
  <table style="width:100%;">
    {% for line in lines %}
    <tr>
      <td>{{ line.quantity }}</td>
      <td>{{ line.product_name }}</td>
      <td style="text-align:right;">{{ line.total_price }}</td>
    </tr>
    {% endfor %}
  </table>

  <hr/>

  <!-- TOTALES -->
  <div>
    <div style="display:flex; justify-content:space-between;">
      <span>Subtotal:</span><span>{{ totals.subtotal }}</span>
    </div>
    <div style="display:flex; justify-content:space-between;">
      <span>{{ totals.tax_name }}:</span><span>{{ totals.tax_amount }}</span>
    </div>
    <div style="display:flex; justify-content:space-between; font-weight:bold;">
      <span>TOTAL:</span><span>{{ totals.total }}</span>
    </div>
  </div>

  <hr/>

  <!-- PAGOS -->
  {% for payment in payments %}
  <div style="display:flex; justify-content:space-between;">
    <span>{{ payment.name }}:</span><span>{{ payment.amount }}</span>
  </div>
  {% endfor %}
  {% if totals.change %}
  <div style="display:flex; justify-content:space-between;">
    <span>Cambio:</span><span>{{ totals.change }}</span>
  </div>
  {% endif %}

  {% if config.footer_message %}
  <hr/>
  <div style="text-align:center;">{{ config.footer_message }}</div>
  {% endif %}

</div>]]></field>
        </record>
    </data>

    <!-- Cron job para reinicio automático de correlativo (se actualiza en cada upgrade) -->
    <data noupdate="0">
        <record id="ir_cron_ticket_sequence_reset" model="ir.cron">
            <field name="name">POS — Reinicio automático de correlativo de tickets</field>
            <field name="model_id" ref="point_of_sale.model_pos_config"/>
            <field name="state">code</field>
            <field name="code">model._cron_reset_ticket_sequences()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active">True</field>
            <field name="user_id" ref="base.user_root"/>
        </record>
    </data>

</odoo>
